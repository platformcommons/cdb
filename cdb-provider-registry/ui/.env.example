# Vite environment variables for UI
# Copy this file to `.env` and adjust values as needed.
# Vite will expose variables prefixed with VITE_ to the client code.

# Base URL of the backend API Gateway or Provider Registry service
# Examples:
#   http://localhost:8080
#   https://api.my-domain.example
VITE_API_BASE_URL=http://localhost:8081

# Authentication mode: 'oauth2' (default) or 'direct'
VITE_AUTH_MODE=oauth2

# CDB Auth Registry base URL (used for OAuth2 authorize/token)
VITE_CDB_AUTH_URL=http://localhost:8083

# OAuth2 client ID for Provider Registry UI
VITE_OAUTH_CLIENT_ID=cdb_provider_registry
