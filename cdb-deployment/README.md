# CDB Deployment

This module is intended for deployment artifacts such as Dockerfiles, Helm charts, Kubernetes manifests, or other orchestration scripts.

Suggested structure:
- `src/main/resources/helm/` for Helm charts
- `src/main/resources/k8s/` for Kubernetes manifests
- `src/main/resources/docker/` for Docker-related assets
