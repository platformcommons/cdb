# CDB Auth Registry

Spring Boot service providing user, application, roles, permissions, authentication, and authorization management for the CDB ecosystem. Includes JWT/OAuth2 support and auditing hooks.
