/**
 * Repository abstractions for the CDB API Registry.
 * <p>
 * These are intentionally technology-agnostic interfaces to allow swapping
 * between different persistence backends (JPA, MongoDB, Elasticsearch, etc.).
 * </p>
 */
package com.platformcommons.cdb.platform.api.registry.repository;
